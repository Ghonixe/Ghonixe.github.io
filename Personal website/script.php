<?php
$name = $_POST['name'];
$email = $_POST['email'];
$message = $_POST['message'];

// Use the PHP mail() function or a third-party library to send an email
// with the user's name, email address, and message

?>